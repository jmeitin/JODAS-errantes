import Object from './Object.js';

export default class Gun extends Object{
    constructor(scene, x, y, type, peso){
        super(scene, x, y, type, peso);
        bool 
    }

    killPlayer(){

    }
}